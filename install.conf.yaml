- clean: ["~"]
- link:
    ~/.dotfiles: ''
    ~/.bashrc: bashrc
    ~/.profile: profile
    ~/.bash_aliases: bash_aliases
    ~/.bash-local: bash-local
    ~/.bcrc: bcrc 
    ~/.dircolors: dircolors
    ~/.gemrc: gemrc
    ~/.git.scmbrc: git.scmbrc
    ~/.gitconfig: gitconfig
    ~/.gitignore: gitignore
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: vimrc
    ~/.vim: vim/
    ~/.ssh: ssh
