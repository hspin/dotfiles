#!/usr/bin/env bash

# bash-local
# export FZF_DEFAULT_COMMAND='ag --hidden --ignore .git -g ""'
# export FZF_DEFAULT_OPTS='--height 40% --reverse --border'

npm install --only=dev eslint babel-eslint eslint-plugin-react
npm install --only=dev prettier eslint-config-prettier eslint-plugin-prettier

# eslint --init
